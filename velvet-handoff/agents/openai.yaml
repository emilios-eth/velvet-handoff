interface:
  display_name: "Velvet Handoff"
  short_description: "Repo-grounded pre-handoff audit"
  default_prompt: "Run $velvet-handoff on this plan. Find the repo north star, flag unmaterialized chat decisions, and return prioritized findings with concrete fixes before implementation."
