interface:
  display_name: "Velvet Handoff"
  short_description: "Audit plans and prepare implementation packets"
  default_prompt: "Run $velvet-handoff on this plan. Create or update the implementation handoff packet, but do not start implementation."
